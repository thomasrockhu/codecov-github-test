container:
  image: python:slim

test_task:
  pip_cache:
    folder: ~/.cache/pip
    populate_script: pip install coverage
  test_script: coverage run tests.py
  upload_coverage_script: bash <(curl -s https://codecov.io/bash) -X s3
